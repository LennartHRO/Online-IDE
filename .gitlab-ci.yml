# no image due to shell executor

stages:
  - build
  - test
  - package

# for debugging
before_script:
  #- alias mvn="/usr/local/apache-maven/apache-maven-3.9.6/bin/mvn"
  # unfortunatly this is not working
  - /usr/local/apache-maven/apache-maven-3.9.6/bin/mvn --version

# build job
build:
  stage: build
  script: 
  - /usr/local/apache-maven/apache-maven-3.9.6/bin/mvn clean compile -f ./compiler
  - /usr/local/apache-maven/apache-maven-3.9.6/bin/mvn clean compile -f ./dark-mode
  - /usr/local/apache-maven/apache-maven-3.9.6/bin/mvn clean compile -f ./project
  - /usr/local/apache-maven/apache-maven-3.9.6/bin/mvn clean compile -f ./security
  - /usr/local/apache-maven/apache-maven-3.9.6/bin/mvn clean compile -f ./UI

# test Job
test:
  stage: test
  script: 
  - /usr/local/apache-maven/apache-maven-3.9.6/bin/mvn test -f ./compiler

# package Job
package:
  stage: package
  script: /usr/local/apache-maven/apache-maven-3.9.6/bin/mvn package
  variables:
    MAVEN_OPTS: "-DskipTests=true"
